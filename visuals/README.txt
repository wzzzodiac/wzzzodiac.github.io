W.ZC VISUAL ARCHIVE

hub #1
Exact snapshot of the currently deployed visual system and page markup. Keep as rollback/reference material.

hub #2
Official next visual direction. Contains a design specification plus a standalone HTML/CSS prototype. It is intentionally not connected to the live hub yet.

Rule: never overwrite an older visual folder when starting a new visual direction. Archive it first, then work on the next numbered hub.